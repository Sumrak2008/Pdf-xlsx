# Runtime dependencies of the application itself.
# Exact versions are pinned for reproducible offline builds.
# These packages (and their own transitive dependencies) are the ONLY
# third-party Python code bundled into the final Windows program.
PySide6==6.8.2.1
pdfplumber==0.11.5
pypdfium2==4.30.0
pytesseract==0.3.13
openpyxl==3.1.5
opencv-python-headless==4.10.0.84
numpy==2.1.3
Pillow==11.0.0
